@model IEnumerable<LTC1070.Models.LTC070>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <label>Mã số</label>
            </th>
            <th>
                <label>Họ và tên</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LTCId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LTCName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LTCId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LTCId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LTCId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
